{
  "name": "stream-try-catch",
  "version": "0.0.2",
  "description": "Simulate a try catch on a stream pipeline",
  "repository": "jeromew/stream-try-catch",
  "dependencies": {
  },
  "devDependencies": {
    "mocha": "~1.17.0"
  },
  "scripts": {
    "test": "make test"
  }
}
